/* El dueño de un restaurante entrevista a cinco clientes y les pide que califiquen
(con puntaje de 1 a 10) los siguientes aspectos: (0) Atención al cliente (1) Calidad
de la comida (2) Precio (3) Ambiente.
Escriba un programa que lea desde teclado las calificaciones de los cinco clientes
para cada uno de los aspectos y almacene la información en una estructura. Luego
imprima la calificación promedio obtenida por cada aspecto. */
package java1;
import PaqueteLectura.Lector;
public class Ejercicio5 {

    public static void main(String[] args) {
    
    int dFCliente = 5;
    int dFAspecto = 4;
    int [][] matriz = new int [dFCliente][dFAspecto];
        
    int i = 0;
    int j = 0;
    int atencion; int calidadComida; int precio; int ambiente;
    for (i=0; i<dFCliente; i++) {
        System.out.println("Ingrese el puntaje para la atencion al cliente: ");
        atencion = Lector.leerInt();
        matriz[i][0] = atencion;
        System.out.println("Ingrese el puntaje para la calidad de la comida: ");
        calidadComida = Lector.leerInt();
        matriz[i][1] = calidadComida;
        System.out.println("Ingrese el puntaje para el precio: ");
        precio = Lector.leerInt();
        matriz[i][2] = precio;
        System.out.println("Ingrese el puntaje para el ambiente: ");
        ambiente = Lector.leerInt();
        matriz[i][3] = ambiente;
    }
    int calificacionPromedio;
    int promedioTotal;
    for (i=0; i<dFCliente; i++) {
        calificacionPromedio = 0;
        for (j=0; j<dFAspecto; j++) 
            calificacionPromedio = calificacionPromedio + matriz[i][j];
        promedioTotal = calificacionPromedio/dFAspecto;
        System.out.println("La calificacion promedio del cliente " + i + " es: " + promedioTotal);
    }
    }
}
