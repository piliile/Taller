/*2- Escriba un programa que lea las alturas de los 15 jugadores de un equipo de
básquet y las almacene en un vector. Luego informe:
- la altura promedio
- la cantidad de jugadores con altura por encima del promedio
NOTA: Dispone de un esqueleto para este programa en Ej02Jugadores.java */
package java1;
import PaqueteLectura.Lector;
public class Ejercicio2 {
    public static void main(String[] args) {
    
        int dF = 15;
        double [] vector = new double [dF];
            
        int i;
        double altura;
        double sumaAlturas = 0;
        
        for (i = 0; i < dF; i++) {
            System.out.println("Ingrese una altura: ");
            altura = Lector.leerDouble();
            vector[i] = altura;
            sumaAlturas = sumaAlturas + altura;
        }

        double alturaPromedio = sumaAlturas/dF;
        System.out.println("El promedio de las alturas es: " + alturaPromedio);
        
        int cantidadJugadores = 0;
        for (i = 0; i < dF; i++) {
            if (vector[i] > alturaPromedio) 
                cantidadJugadores++;
        }
        System.out.println("La cantidad de jugadores que superan la altura " + alturaPromedio +", son: " + cantidadJugadores);   
    }
}
