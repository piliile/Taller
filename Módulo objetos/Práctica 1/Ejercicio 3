/* 3- Escriba un programa que defina una matriz de enteros de tamaño 5x5. Inicialice
la matriz con números aleatorios entre 0 y 30.
Luego realice las siguientes operaciones:
- Mostrar el contenido de la matriz en consola.
- Calcular e informar la suma de los elementos de la fila 1
- Generar un vector de 5 posiciones donde cada posición j contiene la suma
de los elementos de la columna j de la matriz. Luego, imprima el vector.
- Leer un valor entero e indicar si se encuentra o no en la matriz. En caso de
encontrarse indique su ubicación (fila y columna) en caso contrario
imprima “No se encontró el elemento”.
NOTA: Dispone de un esqueleto para este programa en Ej03Matrices.java */
package java1;
import PaqueteLectura.GeneradorAleatorio;
import PaqueteLectura.Lector;
public class Ejercicio3 {

    public static void main(String[] args) {
        GeneradorAleatorio.iniciar();
        
        int dF = 5;
        int dF2 = 5;
        int [][] matriz = new int [dF][dF2];
        
        int i = 0; 
        int j = 0;
        int ale = GeneradorAleatorio.generarInt(31);
        for (i=0; i<dF; i++) {
            for (j=0; j<dF2; j++) {
                matriz[i][j] = ale;
                ale = GeneradorAleatorio.generarInt(31);
            }
        }   
        
        System.out.println("La matriz impresa quedaría de la siguiente manera: ");
        for (i=0; i<dF; i++) {
            for (j=0; j<dF2; j++) {
                System.out.println("En la fila: " + i + ", columna: " + j + " se encuentra el numero: " + matriz[i][j]);
                System.out.println("_________________________________________________________________");
            }
        }
        
        int sumaFilaUno = 0;
        for (j=0; j<dF2; j++) 
            sumaFilaUno = sumaFilaUno + matriz[1][j]; //matriz[1][j]?? o matriz[0][j]??
        System.out.println("La suma de los elementos de la fila 1 es: " + sumaFilaUno);
        
        int [] vector = new int [dF2];
        
        
        for (j=0; j<dF2; j++) {
            int sumaParaVector = 0;
            for (i=0; i<dF; i++) 
                sumaParaVector = sumaParaVector + matriz[i][j];
            vector[j] = sumaParaVector;
        }
       
        System.out.println("El vector cargado con cada posicion correspondiente a la columna j quedaría de la siguiente manera: ");
        for (j=0; j<dF2; j++) {
            System.out.println(vector[j]);
            System.out.println("-------------");
        }
        
        System.out.println("Ingrese un numero para buscarlo en la matriz: ");
        int numero = Lector.leerInt();
        int filaNumero = -1;
        int columnaNumero = -1;
        boolean encontre = false;
        i = 0;
        j = 0;
        while (i<dF && !encontre) {
            while (j <dF2 && !encontre) {
                if (matriz[i][j] == numero) {
                    encontre = true;
                    filaNumero = i; 
                    columnaNumero = j;
                }
                else
                    j++;
            }
            if (!encontre) {
                i++;
                j = 0;
            }
        }
        if (encontre == true) 
            System.out.println("El número " + numero + " está en la matriz, en la posición, fila " + filaNumero + ", columna " + columnaNumero);
        else
            System.out.println("El número " + numero + " no se encuentra en la matriz");
    }
    
}
