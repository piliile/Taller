package MisClases;

public class AutorA {
    // definimos los atributos privados.
    private String nombre;
    private String biografia;
    private String origen;
    // realizamos el constructor (ayuda de java).
    public AutorA(String nombre, String biografia, String origen) {
        this.nombre = nombre;
        this.biografia = biografia;
        this.origen = origen;
    }
    
    public AutorA(){
        
    }
    // definimos los getters y setters (ayuda de java).
    public String getNombre() {
        return nombre;
    }

    public void setNombre(String nombre) {
        this.nombre = nombre;
    }

    public String getBiografia() {
        return biografia;
    }

    public void setBiografia(String biografia) {
        this.biografia = biografia;
    }

    public String getOrigen() {
        return origen;
    }

    public void setOrigen(String origen) {
        this.origen = origen;
    }
    // definimos el metodo toString().
    @Override
    public String toString(){
        String aux;
        aux= " el autor " + nombre + ". Con biografía " + biografia + ", y origen " + origen;
       return ( aux);
    }
    // como no pide ningun otro metodo para realizar en la clase, terminamos de construir la clase Autor.
    
}
